# Android VPN runtime assets

Large prebuilt native VPN cores are intentionally excluded from the public source tree. Use the Flutter plugin's supported build flow or place audited, architecture-specific runtime artifacts here when preparing a distribution build.
